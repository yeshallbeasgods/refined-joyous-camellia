# Read by the Allure JUnit 5 listener at test time.
# Must match the directory Surefire is told to use in pom.xml, so results
# land in one place regardless of how tests are launched (mvn, IDE, CI).
allure.results.directory=target/allure-results
